# Firebase Configuration (for Vercel deployment)
# Generate a service account key from Firebase Console and encode it as base64
# To create the base64 string: cat your-service-account-key.json | base64
FIREBASE_SERVICE_ACCOUNT_KEY=your_base64_encoded_firebase_service_account_key_here
FIREBASE_DATABASE_URL=https://your-project.firebaseio.com

# Local development (optional)
# If you want to test Firebase locally, add the above variables
# Otherwise, leave them blank to use localStorage fallback
